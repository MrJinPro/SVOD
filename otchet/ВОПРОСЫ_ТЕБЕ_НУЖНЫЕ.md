# ВОПРОСЫ ДЛЯ УТОЧНЕНИЯ

**Дата:** 29 апреля 2026

## Короткий вывод по папке `damp_db_agency`

- Таблица `engineers` есть, но в сырой схеме нет прямой связи `event → engineer`.
- Таблица `GroupResponse` есть, и у неё есть `Event_id` + `TimeArriveToObject` (время прибытия ГБР).
- Таблица `eventservice20260201` содержит `PersonName` (оператор) и `GrResponseName` (ГБР). В данных `GrResponseName` заполнен на этапах `Выслана группа реагирования` / `Прибытие группы реагирования`.
- Таблица `Temp` (archive) содержит `Panel_id`, `Line`, `Zone`, `Code`, `TimeEvent`, `MeterCount` (текстовое описание с штрафами и результатами).
- Таблица `Zones` содержит описание шлейфа (`Message`) для комбинации `Panel_id + Zone`.
- В `archive20260201.sql` есть текстовые описания с упоминанием штрафов ("Штраф №12225") и результатов ("Осмотрено").
- В `StatusGroupResponse` есть статусы ГБР ("Свободна", "На выезде", "На объекте", "Осмотр объекта").

## Вопросы, которые мне реально нужны

1. **Шлейф**

   - В `Temp` есть `Line` (varchar) и `Zone` (int), в `Zones` есть `Message` (описание шлейфа).
   - Как именно формируется поле `Шлейф` в отчёте? `Temp.Line`, `Temp.Zone`, `Zones.Message`, или комбинация?
     Ответ : уточни в каком отчёте чтоб я смог проверить в ручную и дам точный ответ
2. **Инженер**

   - Таблица `engineers` есть с именами, но нет прямой связи с событием.
   - Как определить, какой инженер работал по конкретному событию? Через `Panel_id` из `Temp` → `Work_panel_id` в `engineers`?
3. **ГБР**

   - В `eventservice` есть `GrResponseName` (не всегда NULL), в `GroupResponse` есть `Description` и связь по `Event_id`.
   - В выборке есть живые значения `GrResponseName` для состояний `Выслана группа реагирования` и `Прибытие группы реагирования`.
   - Что брать для поля `ГБР` в отчёте: `eventservice.GrResponseName` или `GroupResponse.Description`?
4. **Смена**

   - В `eventservice` есть `PersonName` (оператор), но нет явного поля смены.
   - Как определить смену: по времени `OperationTime` (день 09:00–20:00, ночь 20:00–09:00), или по логину оператора?
5. **Штраф**

   - В `archive` есть текстовые описания типа "Штраф №12225 Юр" в поле `MeterCount`.
   - Это всегда 0 как в примере, или нужно парсить текст из `MeterCount`? Или есть отдельное поле?
6. **Результат осмотра**

   - В `eventservice` есть `NameState` ("Окончание обработки", "Отмена тревоги").
   - В `StatusGroupResponse` есть статусы ("Осмотр объекта", "На объекте").
   - В `archive` есть текстовые описания ("Осмотрено, на объекте...").
   - Что брать: `eventservice.NameState`, статус из `StatusGroupResponse`, или текст из `MeterCount`?

## Почему остальные вопросы пока не нужны

- Я уже вижу, что `engineers`, `GroupResponse`, `Temp` и `eventservice` есть.
- Поэтому сейчас важнее уточнить именно эти 6 пунктов.
- Остальные детали (индикаторы, фильтры, итоговые строки) можно доделать после этих ответов.

---

**Обновлено 29 апреля 2026:** Проверил `archive20260201.sql` и `eventservice20260201.sql` - подтвердил наличие полей для штрафов и результатов осмотра в текстовых описаниях. Обновил вопросы с конкретными примерами из SQL.
